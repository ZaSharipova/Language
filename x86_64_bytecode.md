# Методичка: x86-64 байт-код и формат ELF

Эта методичка описывает, как руками собирать машинный код архитектуры
x86-64 и как упаковывать его в исполняемый
файл формата ELF. Цель — научиться по тексту инструкции получать ровно
те байты, которые исполняет процессор, и наоборот — читать дамп
`objdump -d`, `readelf -a` или hex-вывод как обычный текст.

Материал ориентирован на 64-битный long mode под Linux (System V AMD64
ABI).

---

## Оглавление

### Часть I. Анатомия инструкции

- [Глава 1. Структура инструкции](#глава-1-структура-инструкции)
- [Глава 2. Legacy-префиксы](#глава-2-legacy-префиксы)
- [Глава 3. REX-префикс](#глава-3-rex-префикс)
- [Глава 4. ModRM-байт](#глава-4-modrm-байт)
- [Глава 5. SIB-байт](#глава-5-sib-байт)
- [Глава 6. RIP-relative адресация](#глава-6-rip-relative-адресация)

### Часть II. Регистры и инструкции

- [Глава 7. Таблица регистров x86-64](#глава-7-таблица-регистров-x86-64)
- [Глава 8. MOV — пересылка данных](#глава-8-mov--пересылка-данных)
- [Глава 9. PUSH / POP](#глава-9-push--pop)
- [Глава 10. Арифметика и логика](#глава-10-арифметика-и-логика)
- [Глава 11. LEA — вычисление адреса](#глава-11-lea--вычисление-адреса)
- [Глава 12. Переходы и вызовы](#глава-12-переходы-и-вызовы)
- [Глава 13. ret и syscall](#глава-13-ret-и-syscall)

### Часть III. Соглашения и практика

- [Глава 14. System V AMD64 ABI](#глава-14-system-v-amd64-abi)
- [Глава 15. Endianness и кодирование чисел](#глава-15-endianness-и-кодирование-чисел)

### Часть IV. Формат ELF

- [Глава 16. Что такое ELF и зачем он нужен](#глава-16-что-такое-elf-и-зачем-он-нужен)
- [Глава 17. ELF Header (Ehdr)](#глава-17-elf-header-ehdr)
- [Глава 18. Program Headers (Phdr)](#глава-18-program-headers-phdr)
- [Глава 19. Section Headers (Shdr)](#глава-19-section-headers-shdr)
- [Глава 20. Виртуальные адреса, выравнивание и загрузка в память](#глава-20-виртуальные-адреса-выравнивание-и-загрузка-в-память)
- [Глава 21. Минимальный ELF-исполняемый файл](#глава-21-минимальный-elf-исполняемый-файл)

### Часть V. Справочники

- [Глава 22. Полная шпаргалка](#глава-22-полная-шпаргалка)
- [Глава 23. Где читать дальше](#глава-23-где-читать-дальше)

---

# Часть I. Анатомия инструкции

## Глава 1. Структура инструкции

Каждая инструкция x86-64 состоит из необязательных полей в строгом
порядке:

```
[Legacy prefixes] [REX] [OPCODE] [ModRM] [SIB] [disp8/32] [imm8/32/64]
```

Обязателен только опкод. Остальные поля появляются по необходимости.
Максимальная длина одной инструкции в x86-64 — **15 байт**; всe, что
длиннее, процессор отвергает как `#UD` (invalid opcode).

Декодер процессора читает байты слева направо строго по этой схеме.
Никаких разделителей нет: понять, где кончается одна инструкция и
начинается следующая, можно только зная длину каждого поля.

---

## Глава 2. Legacy-префиксы

До REX перед опкодом могут стоять необязательные «классические»
префиксы. Их можно делить на четыре группы; в одной инструкции
допускается не больше одного префикса из каждой группы.

| Префикс | Группа | Назначение |
|---------|--------|------------|
| `F0`    | LOCK   | атомарная операция (`lock add`, `lock cmpxchg`) |
| `F2`    | REPNE / SSE | `repne`, либо обязательный prefix для SSE double |
| `F3`    | REP / SSE | `rep movs`, `rep stos`, либо prefix для SSE single |
| `2E`/`36`/`3E`/`26`/`64`/`65` | Segment override | в long mode реально работают только `FS` (`64`) и `GS` (`65`) |
| `66`    | Operand-size | переключает размер операнда (см. ниже) |
| `67`    | Address-size | переключает размер адреса (32 → используются регистры `eXX`) |

### 2.1. Префикс `0x66` — operand-size override

В long mode по умолчанию операнды 32-битные. `0x66` переключает на
**16 бит**:

```
01 D8       ; add eax, ebx       (32 бит)
66 01 D8    ; add  ax,  bx       (16 бит, к тому же опкоду добавили 66)
48 01 D8    ; add rax, rbx       (64 бит, REX.W вместо 66)
```

Тот же байт `0x66` для SSE-инструкций служит «обязательным префиксом»,
переключающим mnemonic (`movdqa` vs `movdqu` и т.п.).

### 2.2. Префиксы `0xF2`/`0xF3` для SSE

Скалярные SSE-инструкции имеют общий двухбайтный опкод `0F XX`, а
prefix-байт уточняет тип данных:

| Prefix | Тип | Пример |
|--------|-----|--------|
| `F3`   | scalar single (float) | `addss xmm0, xmm1` → `F3 0F 58 C1` |
| `F2`   | scalar double | `addsd xmm0, xmm1` → `F2 0F 58 C1` |
| `66`   | packed double | `addpd xmm0, xmm1` → `66 0F 58 C1` |
| (нет)  | packed single | `addps xmm0, xmm1` → `0F 58 C1` |

---

## Глава 3. REX-префикс

### 3.1. Откуда взялся REX

Исторически x86 использовал 8 регистров (`eax`–`edi`) и 32-битные
операнды. Когда появился x86-64, нужно было добавить ещe 8 регистров
(`r8`–`r15`) и 64-битный режим — но менять длину существующих
инструкций нельзя, иначе сломается весь старый код. Решение:
специальный префиксный байт, который процессор читает перед
инструкцией и меняет еe поведение.

### 3.2. Структура байта REX

REX — это всегда один байт в диапазоне `0x40`–`0x4F`. Старшие 4 бита
фиксированы (`0100`), младшие 4 — флаги:

```
 7   6   5   4   3   2   1   0
+---+---+---+---+---+---+---+---+
| 0 | 1 | 0 | 0 | W | R | X | B |
+---+---+---+---+---+---+---+---+
  фиксировано: 0100    флаги WRXB
```

Поскольку старшие 4 бита всегда `0100` = `0x40`, итоговый байт
вычисляется как:

```
REX = 0x40 | (W << 3) | (R << 2) | (X << 1) | B
```

### 3.3. Что означает каждый флаг

**W — Wide (бит 3, маска `0x08`)**

Переключает операнды с 32-битных на 64-битные.

```
без REX:   add eax, ebx   — 32-битное сложение, результат в eax (32 бит)
с REX.W:   add rax, rbx   — 64-битное сложение, результат в rax (64 бит)
```

Когда W=0 и REX присутствует — он нужен только для доступа к `r8`–`r15`
(или для адресации `spl`/`bpl`/`sil`/`dil` — см. ниже), но ширина
остаeтся 32-битной.

**R — Extension of ModRM.reg (бит 2, маска `0x04`)**

В ModRM под поле `reg` отведено только 3 бита — это коды 0–7
(`rax`–`rdi`). Чтобы закодировать `r8`–`r15` (коды 8–15), нужен ещe
один бит. REX.R становится старшим (4-м) битом поля `reg`:

```
reg в ModRM = REX.R * 8 + ModRM.reg[2:0]
```

**X — Extension of SIB.index (бит 1, маска `0x02`)**

То же самое, но для поля `index` внутри SIB-байта. Позволяет
использовать `r8`–`r15` в качестве индексного регистра при
масштабированной адресации.

**B — Extension of ModRM.rm / SIB.base / опкода (бит 0, маска `0x01`)**

Расширяет поле `rm` в ModRM, поле `base` в SIB, или номер регистра,
встроенный в опкод (для `push`/`pop`/`mov reg,imm`).

### 3.4. Вычисление REX от руки — алгоритм

1. Начать с `0x40`.
2. Нужны 64-битные операнды? → прибавить `0x08` (W=1).
3. Регистр в поле `reg` ModRM ≥ 8? → прибавить `0x04` (R=1).
4. Регистр в поле `index` SIB ≥ 8? → прибавить `0x02` (X=1).
5. Регистр в поле `rm`/`base`/опкоде ≥ 8? → прибавить `0x01` (B=1).

Пример: `mov rdi, r12` — 64-бит, reg=rdi(7), rm=r12(12):

```
0x40
+ 0x08  (W=1, 64-бит)
+ 0x00  (R=0, rdi=7 < 8)
+ 0x01  (B=1, r12=12 ≥ 8)
= 0x49
```

(Чтобы получить `0x4C`, надо чтобы расширение было у `reg`, а не у
`rm`. Кодировка `mov r/m64, r64` — опкод `89` — кладeт источник в
`reg`, получатель в `rm`. Если же использовать `mov r64, r/m64` —
опкод `8B` — наоборот.)

Пример: `push r13` — push в long mode всегда 64-битный, REX.W не
нужен; регистр в опкоде = r13 (13):

```
0x40
+ 0x00  (W=0)
+ 0x01  (B=1, r13 ≥ 8)
= 0x41
```

### 3.5. Таблица часто встречающихся REX-байт

| Байт   | Биты WRXB | Когда использовать |
|--------|-----------|--------------------|
| `0x40` | 0000 | REX без флагов (нужен для `spl`/`bpl`/`sil`/`dil`) |
| `0x41` | 0001 | push/pop r8–r15, rm/base = r8–r15 без 64-бит |
| `0x44` | 0100 | reg из r8–r15, операнды 32-бит |
| `0x45` | 0101 | оба из r8–r15, 32-бит |
| `0x48` | 1000 | 64-бит, регистры rax–rdi |
| `0x49` | 1001 | 64-бит, rm/base = r8–r15 |
| `0x4C` | 1100 | 64-бит, reg = r8–r15 |
| `0x4D` | 1101 | 64-бит, оба из r8–r15 |
| `0x4E` | 1110 | 64-бит, reg и index из r8–r15 |
| `0x4F` | 1111 | 64-бит, все три из r8–r15 |

### 3.6. Тонкость: REX и младшие байты

Без REX доступ к младшему байту регистра выглядит так:
`AL`/`CL`/`DL`/`BL` (коды 0–3) и `AH`/`CH`/`DH`/`BH` (коды 4–7).
В присутствии любого REX (даже `0x40`) коды 4–7 в low-byte режиме
переключаются на `SPL`/`BPL`/`SIL`/`DIL` — младшие байты `rsp`/`rbp`/
`rsi`/`rdi`. Поэтому к `AH/CH/DH/BH` обращаться при наличии REX уже
нельзя.

---

## Глава 4. ModRM-байт

### 4.1. Зачем нужен ModRM

Процессор должен знать: где находятся операнды инструкции — в регистрах
или в памяти, и какие именно. Один байт опкода не может содержать всю
эту информацию. ModRM — дополнительный байт, который это уточняет.

### 4.2. Структура байта ModRM

```
 7   6   5   4   3   2   1   0
+---+---+---+---+---+---+---+---+
|  mod  |    reg    |    rm     |
+---+---+---+---+---+---+---+---+
  2 бита   3 бита     3 бита
```

Формула сборки:

```c
uint8_t ModRM(int mod, int reg, int rm) {
    return (uint8_t)((mod << 6) | ((reg & 7) << 3) | (rm & 7));
}
```

### 4.3. Поле mod — режим адресации (2 бита)

`mod` определяет, как интерпретировать поле `rm`:

| mod | Двоичное | Смысл |
|-----|----------|-------|
| 0   | `00`     | `rm` — адрес в памяти: `[reg_rm]`. **Исключения:** rm=4 → читай SIB; rm=5 → `[rip+disp32]` |
| 1   | `01`     | адрес с коротким смещением: `[reg_rm + disp8]` (signed, диапазон −128..127) |
| 2   | `10`     | адрес с длинным смещением: `[reg_rm + disp32]` |
| 3   | `11`     | `rm` — просто регистр, не адрес. Оба операнда — регистры |

Когда нужно работать с двумя регистрами без памяти — всегда `mod=3`.
Это самый частый случай.

### 4.4. Поле reg — регистр или группа опкода (3 бита)

Обычно `reg` задаeт первый (или единственный) регистр-операнд. Но у
некоторых опкодов поле `reg` используется не для регистра, а для
уточнения самой инструкции — это называется `/digit` в документации
Intel:

```
0x83 /0 imm8   — add r/m, imm8
0x83 /5 imm8   — sub r/m, imm8
0x83 /4 imm8   — and r/m, imm8
0xFF /6        — push r/m
0xFF /4        — jmp  r/m
0xFF /2        — call r/m
0xF7 /7        — idiv r/m
0xF7 /3        — neg  r/m
0xC7 /0 imm32  — mov  r/m, imm32
```

### 4.5. Поле rm — второй операнд (3 бита)

`rm` задаeт второй операнд. При `mod=3` это просто регистр. При `mod`
0/1/2 это регистр, содержащий адрес. Особые случаи:

- `rm=4` при `mod≠3`: следующий байт — **SIB**.
- `rm=5` при `mod=0`: это не регистр rbp, а **`[rip + disp32]`**
  (RIP-relative, см. отдельную главу).

### 4.6. Вычисление ModRM от руки

Пример: `add rax, rbx` — оба регистра, rbx пишет в rax.

```
Опкод 0x01 = ADD r/m64, r64    (reg → rm)
mod = 3, reg = rbx = 3, rm = rax = 0
ModRM = (3 << 6) | (3 << 3) | 0 = 0xD8
```

Пример: `mov [rcx], rax` — rax записывается по адресу в rcx.

```
Опкод 0x89 = MOV r/m64, r64    (reg → rm)
mod = 0, reg = rax = 0, rm = rcx = 1
ModRM = 0x01
```

Пример: `sub rsp, 8` — вычитание константы.

```
Опкод 0x83 /5 = SUB r/m64, imm8
mod = 3, reg = 5 (/5 — это SUB, не регистр!), rm = rsp = 4
ModRM = (3 << 6) | (5 << 3) | 4 = 0xEC
```

### 4.7. Таблица ModRM для mod=3 (самые частые)

| Регистры       | mod | reg | rm | ModRM |
|----------------|-----|-----|----|-------|
| rax ← rax      | 3   | 0   | 0  | `0xC0` |
| rax ← rcx      | 3   | 1   | 0  | `0xC8` |
| rax ← rbx      | 3   | 3   | 0  | `0xD8` |
| rbp ← rsp      | 3   | 4   | 5  | `0xE5` |
| rdi ← r12 % 8  | 3   | 4   | 7  | `0xE7` |
| rsp + /0=add   | 3   | 0   | 4  | `0xC4` |
| rsp + /5=sub   | 3   | 5   | 4  | `0xEC` |
| rsp + /4=and   | 3   | 4   | 4  | `0xE4` |

---

## Глава 5. SIB-байт

### 5.1. Зачем нужен SIB

ModRM позволяет адресовать только `[reg]`, `[reg+disp8]`,
`[reg+disp32]`. Но процессору часто нужно обращаться к элементам
массива: `array[i]` — это `base + i * element_size`. Для этого
придумали SIB (Scale-Index-Base) — дополнительный байт, описывающий
более сложную адресацию.

SIB появляется только тогда, когда в ModRM стоит `rm=4` (и `mod≠3`).
Это сигнал: «за ModRM следует SIB-байт».

### 5.2. Структура байта SIB

```
 7   6   5   4   3   2   1   0
+---+---+---+---+---+---+---+---+
| scale |   index   |   base    |
+---+---+---+---+---+---+---+---+
  2 бита   3 бита     3 бита
```

Адрес, который описывает SIB:

```
адрес = base_reg + index_reg * (2 ^ scale) + disp(из ModRM)
```

### 5.3. Поле scale — множитель для index (2 бита)

| scale | Двоичное | Множитель | Для чего |
|-------|----------|-----------|----------|
| 0     | `00`     | ×1        | массив байт (`char[]`) |
| 1     | `01`     | ×2        | массив short (`int16[]`) |
| 2     | `10`     | ×4        | массив int (`int32[]`) |
| 3     | `11`     | ×8        | массив long (`int64[]`, указатели) |

### 5.4. Поле index — индексный регистр (3 бита)

Регистр, который умножается на scale. **Особый случай:** `index=4`
означает «нет индекса» (rsp в роли индекса не используется). Это
позволяет адресовать просто `[base]` через SIB, когда base=rsp (иначе
ModRM не может закодировать `[rsp]`).

### 5.5. Поле base — базовый регистр (3 бита)

Регистр, к которому прибавляется `index * scale`. **Особый случай:**
`base=5` при `mod=0` означает «нет базы» — адрес вычисляется как
`disp32 + index * scale`.

REX.X расширяет поле `index`, REX.B расширяет поле `base` — точно так
же, как REX.R/REX.B расширяют поля ModRM.

### 5.6. Вычисление SIB от руки

Пример: `lea rcx, [rcx + rdi * 8]`

```
base  = rcx = 1, index = rdi = 7, scale = 3 (×8)
SIB = (3 << 6) | (7 << 3) | 1 = 0xF9
```

ModRM перед этим SIB:

```
Опкод 0x8D = LEA r64, m
mod = 0, reg = rcx = 1, rm = 4 (SIB)
ModRM = 0x0C
```

Полная инструкция: `48 8D 0C F9`.

### 5.7. Визуальная схема адресации

```
Полный адрес = disp + base_reg + index_reg * scale

  ModRM.mod  → есть ли disp и какой размер (нет / 8 бит / 32 бит)
  ModRM.rm   → base_reg (или сигнал "читай SIB" если rm=4)
  SIB.base   → base_reg (когда есть SIB)
  SIB.index  → index_reg
  SIB.scale  → множитель для index (1/2/4/8)
```

---

## Глава 6. RIP-relative адресация

В x86-64, в отличие от 32-битного режима, есть отдельный режим
адресации относительно текущего значения регистра-указателя инструкции
(RIP). Кодируется он специальным сочетанием:

```
mod = 0, rm = 5    → [rip + disp32]
```

Раньше (в 32-бит) комбинация `mod=0, rm=5` означала «абсолютный
адрес `[disp32]`». В long mode еe **переиспользовали** под
RIP-relative, потому что position-independent code невозможен без
такого режима.

```
48 8B 05 78 56 34 12   ; mov rax, [rip + 0x12345678]
  48  — REX.W
  8B  — MOV r64, r/m64
  05  — ModRM(0, rax=0, 5 → [rip+disp32])
  78 56 34 12 — disp32 = 0x12345678 (little-endian)
```

Эффективный адрес = (адрес следующей инструкции) + disp32.

Именно через RIP-relative реализуются ссылки на глобальные данные в
PIE-бинарниках и обращения через GOT/PLT.

### 6.1. `[rip + rel32]` через `FF /4`, `FF /2` и `FF /6`

```
FF 25 <rel32>   ; jmp [rip + rel32]    (через GOT — PLT-stub)
FF 15 <rel32>   ; call [rip + rel32]
FF 35 <rel32>   ; push [rip + rel32]
```

ModRM здесь:

```
25 = (0 << 6) | (4 << 3) | 5 = 0x25      (/4 = jmp r/m, rm=5 = [rip+disp32])
15 = (0 << 6) | (2 << 3) | 5 = 0x15      (/2 = call r/m)
35 = (0 << 6) | (6 << 3) | 5 = 0x35      (/6 = push r/m)
```

---

# Часть II. Регистры и инструкции

## Глава 7. Таблица регистров x86-64

### 7.1. Регистры общего назначения

| Код | 64-бит | 32-бит | 16-бит | 8-бит (low) | 8-бит (high*) |
|-----|--------|--------|--------|-------------|---------------|
| 0   | `rax`  | `eax`  | `ax`   | `al`        | `ah` |
| 1   | `rcx`  | `ecx`  | `cx`   | `cl`        | `ch` |
| 2   | `rdx`  | `edx`  | `dx`   | `dl`        | `dh` |
| 3   | `rbx`  | `ebx`  | `bx`   | `bl`        | `bh` |
| 4   | `rsp`  | `esp`  | `sp`   | `spl`†      | — |
| 5   | `rbp`  | `ebp`  | `bp`   | `bpl`†      | — |
| 6   | `rsi`  | `esi`  | `si`   | `sil`†      | — |
| 7   | `rdi`  | `edi`  | `di`   | `dil`†      | — |
| 8   | `r8`   | `r8d`  | `r8w`  | `r8b`       | — |
| 9   | `r9`   | `r9d`  | `r9w`  | `r9b`       | — |
| 10  | `r10`  | `r10d` | `r10w` | `r10b`      | — |
| 11  | `r11`  | `r11d` | `r11w` | `r11b`      | — |
| 12  | `r12`  | `r12d` | `r12w` | `r12b`      | — |
| 13  | `r13`  | `r13d` | `r13w` | `r13b`      | — |
| 14  | `r14`  | `r14d` | `r14w` | `r14b`      | — |
| 15  | `r15`  | `r15d` | `r15w` | `r15b`      | — |

\* `ah/ch/dh/bh` доступны **только без REX**.
† `spl/bpl/sil/dil` доступны **только при наличии REX** (любого, даже
пустого `0x40`).

> Важное свойство 64-битного режима: запись в 32-битную часть
> (например, `mov eax, ...`) **обнуляет верхние 32 бита** регистра.
> Запись в 16- или 8-битную часть верхние биты не трогает.

### 7.2. SSE / AVX

| Код | SSE/AVX | Назначение |
|-----|---------|------------|
| 0–7  | `xmm0`–`xmm7`   | 128-битные SSE-регистры |
| 8–15 | `xmm8`–`xmm15`  | требуют REX/VEX/EVEX |

`ymm0`–`ymm15` — 256-битные (AVX), `zmm0`–`zmm31` — 512-битные
(AVX-512). Кодируются через VEX/EVEX-префиксы (выходят за рамки этой
методички).

---

## Глава 8. MOV — пересылка данных

### 8.1. `mov reg, imm32` (знак-расширение до 64 бит)

```
Опкод: 48 C7 /0 <imm32>
```

```
48 C7 C0 3C 00 00 00   ; mov rax, 60
  48   — REX.W
  C7   — MOV r/m64, imm32 (sign-extend)
  C0   — ModRM(3, 0, rax=0)
  3C 00 00 00 — 60 в little-endian
```

### 8.2. `mov reg, imm64` (полный 64-битный немедленный)

```
Опкод: REX (B8+r) <imm64>
```

```
48 B8 XX XX XX XX XX XX XX XX   ; mov rax, <8 байт>
49 BC XX XX XX XX XX XX XX XX   ; mov r12, <8 байт>
```

> Совет: если `imm` помещается в int32, используйте форму `C7 /0` (7
> байт против 10 у `B8+r`).

### 8.3. `mov reg32, imm32` (без REX)

```
B8+r <imm32>   ; mov eax, imm32  — 5 байт, обнуляет верхние 32 бита rax
```

Часто короче, чем `48 C7 ...`, и работает как ноль-расширение для
неотрицательных значений.

### 8.4. `mov dst, src` (регистр ← регистр)

```
Опкод: REX 89 ModRM(3, src, dst)     ; src → r/m
или:    REX 8B ModRM(3, dst, src)    ; r/m → dst   (эквивалент)
```

```
48 89 E5   ; mov rbp, rsp
  E5  — ModRM(3, rsp=4, rbp=5)

4C 89 E7   ; mov rdi, r12
  4C  — REX.W + REX.R (r12 в reg)
  E7  — ModRM(3, r12%8=4, rdi=7)
```

### 8.5. `mov [mem], rax` и `mov rax, [mem]`

```
48 89 01      ; mov [rcx], rax           (89 = store)
48 8B 01      ; mov rax, [rcx]           (8B = load)
48 8B 45 08   ; mov rax, [rbp + 8]       (mod=1, disp8)
48 89 45 F8   ; mov [rbp - 8], rax       (disp8 = -8 = 0xF8)
48 8B 85 ...  ; mov rax, [rbp + disp32]  (mod=2)
```

> Мнемоника: `0x89` — «src пишет в rm». `0x8B` — «reg читает из rm».

### 8.6. `movsx` / `movzx` — расширение знака и нуля

```
movzx — zero-extend (для unsigned)
movsx — sign-extend (для signed)
movsxd rax, eax  — 64-битная sign-extend форма (опкод 0x63)
```

```
48 63 C0      ; movsxd rax, eax     (sign-extend 32→64)
48 0F BE C0   ; movsx  rax, al      (sign-extend 8→64)
48 0F B6 C0   ; movzx  rax, al      (zero-extend 8→64)
```

---

## Глава 9. PUSH / POP

### 9.1. push/pop для rax–rdi (без REX)

```
Опкод push: 0x50 + код_регистра
Опкод pop:  0x58 + код_регистра
```

| Инструкция | Байты |
|------------|-------|
| `push rax` | `50` |
| `push rbp` | `55` |
| `push rbx` | `53` |
| `push rsp` | `54` |
| `pop rax`  | `58` |
| `pop rbp`  | `5D` |
| `pop rbx`  | `5B` |

### 9.2. push/pop для r8–r15 (с REX.B)

```
Опкод: 41 (50 + код%8)   ; push
Опкод: 41 (58 + код%8)   ; pop
```

| Инструкция | Байты |
|------------|-------|
| `push r12` | `41 54` |
| `push r13` | `41 55` |
| `pop r12`  | `41 5C` |
| `pop r13`  | `41 5D` |

### 9.3. push imm и push из памяти

```
6A 7F                  ; push imm8 (sign-extend до 64)
68 78 56 34 12         ; push imm32 (sign-extend до 64)
FF 31                  ; push qword [rcx]   (/6 = push r/m)
FF 35 <rel32>          ; push qword [rip + rel32]
```

В 64-битном режиме push-pop всегда работает с 8 байтами; 16-битный push
требует префикса `0x66`, 32-битный — недоступен.

---

## Глава 10. Арифметика и логика

### 10.1. add/sub регистр, imm8 (imm в диапазоне −128..127)

```
Опкод: REX 83 /digit reg, imm8
  /0 = add, /1 = or, /4 = and, /5 = sub, /6 = xor, /7 = cmp
```

```
48 83 C4 08   ; add rsp, 8
48 83 EC 08   ; sub rsp, 8
48 83 E4 F0   ; and rsp, -16   (выравнивание стека)
```

### 10.2. add/sub регистр, imm32 (вне диапазона −128..127)

```
Опкод: REX 81 /digit reg <imm32>
```

```
48 81 C4 00 10 00 00   ; add rsp, 0x1000
```

Спецформа для `rax`: `48 05 <imm32>` (`add rax, imm32`) — на 1 байт
короче, потому что не требует ModRM. То же для `sub`/`and`/... — см.
SDM.

### 10.3. add/sub между регистрами

```
48 01 D8   ; add rax, rbx     (01 = ADD r/m64, r64)
48 29 D8   ; sub rax, rbx     (29 = SUB r/m64, r64)
48 03 C3   ; add rax, rbx     (03 = ADD r64, r/m64) — другая форма
```

### 10.4. Умножение

```
48 0F AF C3        ; imul rax, rbx                  (двухоп.)
48 6B C3 0A        ; imul rax, rbx, 10              (трeхоп., imm8)
48 69 C3 78 56 34 12 ; imul rax, rbx, 0x12345678    (трeхоп., imm32)
48 F7 EB           ; imul rbx          (rdx:rax = rax * rbx)
48 F7 E3           ; mul  rbx          (unsigned, rdx:rax = rax * rbx)
```

### 10.5. Деление

Перед `idiv`/`div` обязательно знаковое (или беззнаковое) расширение:

```
48 99         ; cqo   — sign-extend rax в rdx:rax (для idiv)
48 31 D2      ; xor rdx, rdx — для div (unsigned)
48 F7 FB      ; idiv rbx       (signed,   rax = rdx:rax / rbx, rdx = mod)
48 F7 F3      ; div  rbx       (unsigned)
```

### 10.6. inc / dec / neg / not

```
48 FF C0   ; inc rax   (FF /0)
48 FF C8   ; dec rax   (FF /1)
48 F7 D8   ; neg rax   (F7 /3)
48 F7 D0   ; not rax   (F7 /2)
```

> В 32-бит был короткий 1-байтный `inc reg` (`40+r`), но в long mode
> эти опкоды занял REX. Поэтому в x86-64 `inc`/`dec` всегда 3 байта
> через `FF /digit`.

### 10.7. Логика

```
48 21 D8   ; and rax, rbx   (21 = AND r/m64, r64)
48 09 D8   ; or  rax, rbx   (09 = OR)
48 31 D8   ; xor rax, rbx   (31 = XOR)
```

### 10.8. xor для обнуления

```
31 C0   ; xor eax, eax   — обнуляет весь rax (запись в eXX обнуляет верх)
```

Самый короткий способ обнулить регистр (2 байта против 7 у
`mov rax, 0`). Процессор распознаeт это как «zeroing idiom» и
выполняет без чтения исходника.

### 10.9. Сравнение и тест

```
48 39 D8   ; cmp rax, rbx       (39 = CMP r/m64, r64; вычисляет rax-rbx, флаги)
48 85 C0   ; test rax, rax      (85 = TEST; вычисляет AND, флаги; не пишет)
48 83 F8 00 ; cmp rax, 0
```

> `test rax, rax` обычно предпочтительнее `cmp rax, 0` — короче (3 vs 4
> байта) и устанавливает те же ZF/SF.

### 10.10. Сдвиги

```
48 D1 E0   ; shl rax, 1
48 C1 E0 03 ; shl rax, 3   (*8)
48 D3 E0   ; shl rax, cl   (по cl)
```

`/digit`: `/4 = shl/sal`, `/5 = shr`, `/7 = sar`.

### 10.11. setcc и cmovcc

```
0F 9X ModRM   ; setcc r/m8 — установить байт в 1 если условие истинно
0F 4X ModRM   ; cmovcc r, r/m — условный mov
```

Например, `setne al`: `0F 95 C0`.

### 10.12. Операции с плавающей точкой (SSE)

```
F2 48 0F 2A C0   ; cvtsi2sd xmm0, rax   (int64 → double)
F2 0F 51 C0      ; sqrtsd xmm0, xmm0
F2 48 0F 2C C0   ; cvttsd2si rax, xmm0  (double → int64, усечение)
F3 0F 58 C1      ; addss xmm0, xmm1     (float)
F2 0F 58 C1      ; addsd xmm0, xmm1     (double)
```

---

## Глава 11. LEA — вычисление адреса

LEA не обращается к памяти — только вычисляет адрес и кладeт в регистр.
Часто используется как «арифметический комбайн»: позволяет за одну
инструкцию вычислить `r = a + b*scale + disp`.

```
Опкод: REX 8D ModRM [SIB] [disp]
```

```
48 8D 65 E8   ; lea rsp, [rbp - 24]
48 8D 0C F9   ; lea rcx, [rcx + rdi*8]
48 8D 04 5B   ; lea rax, [rbx + rbx*2]   ; rax = rbx * 3
```

---

## Глава 12. Переходы и вызовы

Все переходы используют **относительное смещение** — разницу между
адресом цели и адресом следующей инструкции.

```
rel = target_offset − (instr_offset + длина_инструкции)
```

| Инструкция     | Длина | Опкод |
|----------------|-------|-------|
| `jmp rel8`     | 2     | `EB <rel8>` |
| `jmp rel32`    | 5     | `E9 <rel32>` |
| `call rel32`   | 5     | `E8 <rel32>` |
| `jcc rel8`     | 2     | `7X <rel8>` |
| `jcc rel32`    | 6     | `0F 8X <rel32>` |
| `jmp r/m64`    | 2–    | `FF /4` |
| `call r/m64`   | 2–    | `FF /2` |

В x86-64 нет 16/32-битных near-call/jmp — только rel32 относительно
RIP. Прямой `call imm64` отсутствует: чтобы прыгнуть далеко, надо
загрузить адрес в регистр и сделать `call r64`.

### 12.1. Полная таблица условных переходов (Jcc)

`Jcc rel8` имеет опкод `70 + cc`, `Jcc rel32` — `0F 80 + cc`. Один и
тот же `cc` имеет несколько мнемоник-синонимов.

| Опкод rel32 | Мнемоники         | Условие |
|-------------|-------------------|---------|
| `0F 80`     | `jo`              | OF=1 |
| `0F 81`     | `jno`             | OF=0 |
| `0F 82`     | `jb` / `jc` / `jnae` | CF=1 (unsigned <) |
| `0F 83`     | `jae` / `jnc` / `jnb` | CF=0 (unsigned ≥) |
| `0F 84`     | `je` / `jz`       | ZF=1 |
| `0F 85`     | `jne` / `jnz`     | ZF=0 |
| `0F 86`     | `jbe` / `jna`     | CF=1 OR ZF=1 (unsigned ≤) |
| `0F 87`     | `ja` / `jnbe`     | CF=0 AND ZF=0 (unsigned >) |
| `0F 88`     | `js`              | SF=1 |
| `0F 89`     | `jns`             | SF=0 |
| `0F 8A`     | `jp` / `jpe`      | PF=1 |
| `0F 8B`     | `jnp` / `jpo`     | PF=0 |
| `0F 8C`     | `jl` / `jnge`     | SF≠OF (signed <) |
| `0F 8D`     | `jge` / `jnl`     | SF=OF (signed ≥) |
| `0F 8E`     | `jle` / `jng`     | ZF=1 OR SF≠OF (signed ≤) |
| `0F 8F`     | `jg` / `jnle`     | ZF=0 AND SF=OF (signed >) |

### 12.2. Patch-холдеры для forward-переходов

Когда вы генерируете код в один проход, цель условного перехода часто
ещe неизвестна. Стандартный приeм — записать **нулевой rel32** и
запомнить его позицию; когда цель станет известна, посчитать
смещение и записать его на место плейсхолдера:

```c
// Запоминаем позицию rel32 после опкода
size_t hole = code.size + 1;   // E9 <here>
emit8(0xE9);
emit32(0);                     // временный 0

// ... позже, когда target известен:
int32_t rel = (int32_t)((int64_t)target - (int64_t)(hole + 4));
patch32(hole, (uint32_t)rel);
// +4 потому что RIP в момент исполнения уже указывает за rel32
```

Аналогично для `call rel32` (`E8`) и `Jcc rel32` (`0F 8X`, у которых
плейсхолдер начинается через 2 байта от опкода).

### 12.3. `jmp [rip+rel32]` — PLT-заглушка

```
FF 25 <rel32>   ; jmp qword [rip + rel32]
```

Так реализованы PLT-stubs: они читают адрес функции из GOT и прыгают
туда. Это единственный способ в x86-64 сделать переход на адрес,
лежащий в произвольном месте памяти, не разрушая регистров.

---

## Глава 13. ret и syscall

```
C3        ; ret              — возврат (pop rip)
C2 XX XX  ; ret imm16        — возврат + добавить imm16 к rsp (rare)
0F 05     ; syscall          — системный вызов Linux
CD 80     ; int 0x80         — legacy 32-bit syscall (НЕ используется в x86-64)
```

### 13.1. Соглашение syscall на Linux x86-64

| Регистр | Назначение |
|---------|------------|
| `rax`   | номер syscall / возвращаемое значение |
| `rdi`   | 1-й аргумент |
| `rsi`   | 2-й аргумент |
| `rdx`   | 3-й аргумент |
| `r10`   | 4-й аргумент (не `rcx` — `syscall` затирает `rcx`!) |
| `r8`    | 5-й аргумент |
| `r9`    | 6-й аргумент |

После `syscall` процессор затирает `rcx` (туда сохраняется RIP) и `r11`
(туда сохраняется RFLAGS).

Пример — `exit(0)`:

```
48 31 FF             ; xor rdi, rdi      (код выхода 0)
48 C7 C0 3C 00 00 00 ; mov rax, 60       (номер syscall: exit)
0F 05                ; syscall
```

---

# Часть III. Соглашения и практика

## Глава 14. System V AMD64 ABI

ABI описывает соглашение о вызовах между функциями.

### 14.1. Передача аргументов

Целочисленные/указательные аргументы — через регистры в порядке:

```
rdi, rsi, rdx, rcx, r8, r9
```

Float/double аргументы — через `xmm0`–`xmm7`. Аргументы сверх этого
числа кладутся на стек справа налево.

Возвращаемое значение — `rax` (или `rdx:rax` для 128-бит,
`xmm0`/`xmm0:xmm1` для float).

### 14.2. Регистры

| Регистр | Сохраняет вызывающий (caller-saved) | Сохраняет вызываемый (callee-saved) |
|---------|--------------------------------------|--------------------------------------|
| `rax`, `rcx`, `rdx`, `rsi`, `rdi`, `r8`–`r11`, `xmm0`–`xmm15` | да | — |
| `rbx`, `rbp`, `rsp`, `r12`, `r13`, `r14`, `r15` | — | да |

Если функция использует callee-saved регистр, она обязана сохранить
его (обычно `push reg` в прологе и `pop reg` в эпилоге) и восстановить
до возврата.

### 14.3. Выравнивание стека

Перед `call` стек должен быть выровнен на **16 байт** относительно
адреса возврата: на момент входа в функцию `(rsp + 8) % 16 == 0`.
`call` кладeт 8-байтный return address — после этого `rsp` снова
выровнен на 16. Стандартный пролог `push rbp` сохраняет это
выравнивание.

Для вариативных функций (например, `printf`) с float-аргументами
дополнительно требуется: в `AL` лежит число XMM-регистров,
использованных для аргументов. Если float не передаются, обычно
ставят `xor eax, eax`.

### 14.4. Red zone

Системный V на x86-64 определяет **red zone** — 128 байт ниже `rsp`,
которые могут использоваться функцией без явного `sub rsp, ...`, при
условии что она не вызывает другие функции. Это позволяет
короткие листовые функции писать без пролога.

### 14.5. Типичный пролог и эпилог

```
55           ; push rbp
48 89 E5     ; mov rbp, rsp
48 83 EC 20  ; sub rsp, 32          (если нужны локальные переменные)
   ...
   ...
C9           ; leave   (= mov rsp, rbp; pop rbp)
C3           ; ret
```

Если функция модифицирует callee-saved регистры (`rbx`, `r12`–`r15`),
их push'ат после `mov rbp, rsp` и pop'ят перед `leave`.

---

## Глава 15. Endianness и кодирование чисел

x86-64 — little-endian. Все многобайтные значения (imm32, imm64, disp,
rel) пишутся младшим байтом вперeд:

```
mov eax, 0x12345678  →  B8 78 56 34 12
                          ^^ ^^^^^^^^^^
                       опкод   imm32 (LE)
```

Отрицательные значения — в дополнительном коде:

```
disp8 = -8   → 0xF8
disp8 = -16  → 0xF0
disp8 = -128 → 0x80
imm32 = -1   → FF FF FF FF
```

---

# Часть IV. Формат ELF

## Глава 16. Что такое ELF и зачем он нужен

**ELF** (Executable and Linkable Format) — основной бинарный формат
файлов в Unix-подобных системах: Linux, FreeBSD, Solaris и других. В
ELF упакованы:

- исполняемые файлы (`/usr/bin/ls`),
- объектные файлы — результат компиляции одного `.c` (`foo.o`),
- разделяемые библиотеки (`libc.so.6`),
- core-дампы.

Это один и тот же формат — отличаются только тип файла и набор полей.

### 16.1. Зачем нужен формат вообще

Программа на диске — это не только машинный код. Помимо инструкций
ядру нужно знать:

- какие куски файла загружать в память и с какими правами (R/W/X);
- по каким адресам их класть;
- где точка входа (адрес первой выполняемой инструкции);
- какие библиотеки подгрузить;
- куда вписать адреса внешних функций после загрузки (релокации);
- какие символы экспортирует/импортирует файл.

ELF — это контейнер, который структурно хранит всю эту информацию.
Раньше использовался формат a.out, но он был слишком прост и не
поддерживал динамическую линковку и расширяемость; ELF появился в
System V Release 4 (1989) и стал стандартом.

### 16.2. Две точки зрения на ELF

В одном файле сосуществуют **две** независимые карты содержимого:

```
ELF Header
├── Program Header Table  →  взгляд "загрузчика"  (что грузить в память)
│       ├── PT_LOAD       →  сегмент 1 (например, .text)
│       ├── PT_LOAD       →  сегмент 2 (например, .data)
│       └── PT_INTERP     →  путь к динамическому линкеру
│
├── (содержимое файла — байты кода и данных)
│
└── Section Header Table  →  взгляд "линкера"      (как разбито логически)
        ├── .text
        ├── .data
        ├── .bss
        ├── .symtab
        ├── .strtab
        └── .rela.text
```

Сегменты (Program Headers) описывают, какие куски нужны при запуске —
ядро Linux читает только их. Секции (Section Headers) описывают
логическое разбиение для линкера — `ld`, `objdump`, `readelf` смотрят
именно на них. В исполняемом файле секции необязательны, в объектных
файлах необязательны сегменты.

### 16.3. Структура файла на диске

Типичная компоновка исполняемого ELF:

```
+----------------------+  offset 0
| ELF Header (Ehdr)    |  64 байта
+----------------------+
| Program Headers      |  N × 56 байт
+----------------------+
| segment 1 (code)     |  обычно содержит .text
| ...                  |
+----------------------+
| segment 2 (data)     |  обычно содержит .data, .bss
| ...                  |
+----------------------+
| Section Headers      |  M × 64 байта (могут отсутствовать)
+----------------------+
| .shstrtab, .symtab,  |
| .strtab, …           |  вспомогательные таблицы
+----------------------+
```

Порядок не жeсткий — указатели `e_phoff` и `e_shoff` в Ehdr могут
показывать куда угодно. Но на практике почти всегда так.

---

## Глава 17. ELF Header (Ehdr)

ELF Header — самое начало файла. Из него ядро понимает, что это вообще
ELF, под какую архитектуру он, где найти Program Headers и точку входа.
В 64-битном ELF Ehdr занимает **64 байта**.

### 17.1. Структура Elf64_Ehdr

```c
typedef struct {
    unsigned char e_ident[16];   // "магия" + класс + endianness + версия
    Elf64_Half    e_type;        // тип файла
    Elf64_Half    e_machine;     // архитектура
    Elf64_Word    e_version;     // версия ELF
    Elf64_Addr    e_entry;       // точка входа (виртуальный адрес)
    Elf64_Off     e_phoff;       // offset Program Header Table
    Elf64_Off     e_shoff;       // offset Section Header Table
    Elf64_Word    e_flags;       // архитектурно-специфичные флаги
    Elf64_Half    e_ehsize;      // размер этого заголовка (= 64)
    Elf64_Half    e_phentsize;   // размер одной записи Phdr (= 56)
    Elf64_Half    e_phnum;       // количество Phdr
    Elf64_Half    e_shentsize;   // размер одной записи Shdr (= 64)
    Elf64_Half    e_shnum;       // количество Shdr
    Elf64_Half    e_shstrndx;    // индекс секции с именами секций
} Elf64_Ehdr;
```

Типы (`<elf.h>`):

```
Elf64_Half  — uint16_t  (2 байта)
Elf64_Word  — uint32_t  (4 байта)
Elf64_Addr  — uint64_t  (8 байт, виртуальный адрес)
Elf64_Off   — uint64_t  (8 байт, файловый offset)
```

### 17.2. Поле `e_ident` — первые 16 байт

Самое важное поле, потому что по нему ядро в первую очередь определяет,
что это ELF и какого вида:

| Индекс | Имя константы | Значение | Смысл |
|--------|---------------|----------|-------|
| 0 | `EI_MAG0` | `0x7F` | магическое число |
| 1 | `EI_MAG1` | `'E'` (`0x45`) | ↑ |
| 2 | `EI_MAG2` | `'L'` (`0x4C`) | ↑ |
| 3 | `EI_MAG3` | `'F'` (`0x46`) | ↑ |
| 4 | `EI_CLASS` | `1` или `2` | `ELFCLASS32` / `ELFCLASS64` |
| 5 | `EI_DATA` | `1` или `2` | `ELFDATA2LSB` (LE) / `ELFDATA2MSB` (BE) |
| 6 | `EI_VERSION` | `1` | `EV_CURRENT` |
| 7 | `EI_OSABI` | `0`, `3`, … | ABI: SysV / Linux / FreeBSD / … |
| 8 | `EI_ABIVERSION` | обычно `0` | версия ABI |
| 9–15 | padding | `0` | зарезервировано |

Поэтому первые 4 байта **любого** ELF — это `7F 45 4C 46`. Если открыть
любой бинарник в hex-редакторе, видишь `7F E L F`.

### 17.3. Поле `e_type` — тип файла

| Значение | Имя | Смысл |
|----------|-----|-------|
| 0 | `ET_NONE` | нет типа |
| 1 | `ET_REL`  | relocatable (объектный файл `.o`) |
| 2 | `ET_EXEC` | исполняемый |
| 3 | `ET_DYN`  | shared object (`.so`) или PIE-исполняемый |
| 4 | `ET_CORE` | core-дамп |

### 17.4. Поле `e_machine` — архитектура

| Значение | Имя | Архитектура |
|----------|-----|-------------|
| 3 | `EM_386` | x86 (32-бит) |
| 40 | `EM_ARM` | 32-битный ARM |
| 62 | `EM_X86_64` | x86-64 |
| 183 | `EM_AARCH64` | 64-битный ARM |
| 243 | `EM_RISCV` | RISC-V |

### 17.5. Поле `e_entry` — точка входа

Виртуальный адрес первой инструкции, которая будет выполнена после
загрузки. У `ET_EXEC` это абсолютный адрес (например, `0x400078` для
обычного no-PIE), у `ET_DYN` — относительный (прибавляется база
загрузки).

> Важно различать **файловый offset** и **виртуальный адрес**. `e_entry`
> — именно адрес в памяти, по которому ядро установит RIP после
> загрузки. Сам код может лежать в файле по совсем другому смещению.

### 17.6. Поля `e_phoff`, `e_shoff` и спутники

- `e_phoff` — где в файле начинается Program Header Table. Обычно сразу
  после Ehdr (т.е. = 64).
- `e_phentsize` — размер одной записи Phdr (для ELF64 = 56).
- `e_phnum` — сколько записей в таблице.
- `e_shoff`, `e_shentsize`, `e_shnum` — то же для Section Headers.
- `e_shstrndx` — индекс секции `.shstrtab`, в которой лежат имена всех
  остальных секций (имена секций хранятся не в Shdr, а отдельным
  блобом, в Shdr — только смещения внутрь этого блоба).

Для исполняемого файла, в котором секций нет, `e_shoff = 0`,
`e_shnum = 0`, `e_shstrndx = 0`.

### 17.7. Пример Ehdr — hex-дамп

`readelf -h /bin/ls` для типичного PIE-бинарника даeт что-то такое,
а в файле на диске первые 64 байта выглядят примерно так:

```
00000000  7F 45 4C 46  02 01 01 00  00 00 00 00 00 00 00 00
00000010  03 00       3E 00       01 00 00 00
00000018  60 64 00 00 00 00 00 00                  ; e_entry
00000020  40 00 00 00 00 00 00 00                  ; e_phoff = 64
00000028  78 38 02 00 00 00 00 00                  ; e_shoff
00000030  00 00 00 00                              ; e_flags
00000034  40 00       38 00       0D 00            ; ehsize=64 phentsize=56 phnum=13
0000003A  40 00       1F 00       1E 00            ; shentsize=64 shnum=31 shstrndx=30
```

Разбор первых 16 байт (`e_ident`):

```
7F 45 4C 46   — магия "\x7fELF"
02            — ELFCLASS64
01            — ELFDATA2LSB (little-endian)
01            — EV_CURRENT
00            — ELFOSABI_SYSV (Linux ставит 0, не 3 — это историческое)
00            — ABI version
00 ... 00     — padding
```

---

## Глава 18. Program Headers (Phdr)

Program Header Table описывает **сегменты** — куски файла, которые
нужно отобразить в память при запуске. Это то, что ядро Linux реально
читает при `execve`.

В 64-битном ELF одна запись Phdr — **56 байт**.

### 18.1. Структура Elf64_Phdr

```c
typedef struct {
    Elf64_Word  p_type;     // тип сегмента
    Elf64_Word  p_flags;    // права R/W/X
    Elf64_Off   p_offset;   // offset в файле
    Elf64_Addr  p_vaddr;    // виртуальный адрес в памяти
    Elf64_Addr  p_paddr;    // физический адрес (на ОС с MMU игнорируется)
    Elf64_Xword p_filesz;   // сколько байт занимает в файле
    Elf64_Xword p_memsz;    // сколько байт занимает в памяти
    Elf64_Xword p_align;    // выравнивание (обычно 0x1000 = страница)
} Elf64_Phdr;
```

### 18.2. Поле `p_type` — тип сегмента

| Значение | Имя | Смысл |
|----------|-----|-------|
| 0 | `PT_NULL` | пустая запись, игнорируется |
| 1 | `PT_LOAD` | **загружаемый сегмент** — содержимое отображается в память |
| 2 | `PT_DYNAMIC` | таблица динамической линковки |
| 3 | `PT_INTERP` | путь к динамическому линкеру (например, `/lib64/ld-linux-x86-64.so.2`) |
| 4 | `PT_NOTE` | примечания (например, ID сборки) |
| 6 | `PT_PHDR` | сами Program Headers (для PIE) |
| 7 | `PT_TLS` | thread-local storage |
| `0x6474E550` | `PT_GNU_EH_FRAME` | таблица unwind для exceptions |
| `0x6474E551` | `PT_GNU_STACK` | флаги стека (исполняемый или нет) |
| `0x6474E552` | `PT_GNU_RELRO` | сегмент, который после релокаций станет read-only |

Минимально работоспособному статическому исполняемому файлу хватает
одного-двух `PT_LOAD`. Всe остальное — для динамики и удобства.

### 18.3. Поле `p_flags` — права

Битовая маска:

| Бит | Имя | Значение |
|-----|-----|----------|
| `0x1` | `PF_X` | executable |
| `0x2` | `PF_W` | writable |
| `0x4` | `PF_R` | readable |

Типичные комбинации:

- `PF_R | PF_X` (`5`) — текстовый сегмент (.text, .rodata).
- `PF_R | PF_W` (`6`) — данные (.data, .bss).
- `PF_R | PF_W | PF_X` (`7`) — допустимо, но нежелательно: страница, в
  которой можно и писать, и исполнять, — классический вектор атак. На
  современных системах ядро может ругаться.

### 18.4. `p_offset` vs `p_vaddr` vs `p_paddr`

Это три разные сущности:

- **`p_offset`** — где сегмент лежит в файле (offset от начала ELF).
- **`p_vaddr`** — по какому виртуальному адресу его отобразить.
- **`p_paddr`** — физический адрес. На системах с MMU (Linux, BSD)
  игнорируется; используется только в системах без MMU и в загрузчиках
  ОС, которые работают до включения виртуальной памяти. По соглашению
  обычно равен `p_vaddr`.

Между `p_offset` и `p_vaddr` должно соблюдаться важное соотношение:

```
p_vaddr ≡ p_offset (mod p_align)
```

Иначе ядро откажется загружать файл с `ENOEXEC`. Это связано с тем,
что отображение страниц памяти в файл идeт постранично, и
выравнивания должны совпадать (см. главу 20).

### 18.5. `p_filesz` vs `p_memsz`

- `p_filesz` — сколько байт реально занимает сегмент **в файле**.
- `p_memsz` — сколько байт занимает **в памяти** после загрузки.

Когда `p_memsz > p_filesz`, разница автоматически заполняется нулями
ядром при загрузке. Так реализована секция `.bss` (uninitialized data):
в файле еe нет, но в памяти под неe резервируется место.

### 18.6. `p_align` — выравнивание

Степень двойки. Для `PT_LOAD` обычно равно размеру страницы
(`0x1000` = 4096 на x86-64). Меньшие значения иногда используются
(например, 1 для нессылочных сегментов), но `PT_LOAD` практически
всегда выровнен на страницу.

### 18.7. Пример: типичный PT_LOAD для кода

```c
Elf64_Phdr code_seg = {
    .p_type   = PT_LOAD,
    .p_flags  = PF_R | PF_X,
    .p_offset = 0,                 // включая Ehdr и Phdrs
    .p_vaddr  = 0x400000,
    .p_paddr  = 0x400000,
    .p_filesz = 0x1234,            // код + заголовки
    .p_memsz  = 0x1234,
    .p_align  = 0x1000,
};
```

И сегмент данных:

```c
Elf64_Phdr data_seg = {
    .p_type   = PT_LOAD,
    .p_flags  = PF_R | PF_W,
    .p_offset = 0x2000,            // выровнено на страницу
    .p_vaddr  = 0x402000,          // тоже выровнено
    .p_paddr  = 0x402000,
    .p_filesz = 0x100,             // инициализированные данные
    .p_memsz  = 0x500,             // + .bss (нули) — на 0x400 больше
    .p_align  = 0x1000,
};
```

---

## Глава 19. Section Headers (Shdr)

Section Header Table — взгляд линкера на файл. Делит содержимое на
**секции**, у каждой есть имя, тип, права и расположение. В 64-битном
ELF одна запись Shdr — **64 байта**.

Для исполняемого файла секции необязательны: если их нет, файл всe
равно запустится. Но без них дизассемблеры вроде `objdump` будут
показывать только сырые байты. Объектные файлы (`.o`), напротив, без
Section Headers бесполезны — линкер ничего не сможет с ними сделать.

### 19.1. Структура Elf64_Shdr

```c
typedef struct {
    Elf64_Word  sh_name;       // offset имени в .shstrtab
    Elf64_Word  sh_type;       // тип секции
    Elf64_Xword sh_flags;      // флаги (см. ниже)
    Elf64_Addr  sh_addr;       // виртуальный адрес (если загружается)
    Elf64_Off   sh_offset;     // offset в файле
    Elf64_Xword sh_size;       // размер
    Elf64_Word  sh_link;       // ссылка на другую секцию (зависит от sh_type)
    Elf64_Word  sh_info;       // дополнительная информация
    Elf64_Xword sh_addralign;  // выравнивание
    Elf64_Xword sh_entsize;    // размер записи (для таблиц)
} Elf64_Shdr;
```

### 19.2. Поле `sh_type`

Самые частые:

| Значение | Имя | Смысл |
|----------|-----|-------|
| 0 | `SHT_NULL` | пустая, нулевая запись (обычно индекс 0) |
| 1 | `SHT_PROGBITS` | произвольные байты программы (`.text`, `.data`, `.rodata`) |
| 2 | `SHT_SYMTAB` | таблица символов |
| 3 | `SHT_STRTAB` | таблица строк (имена символов / секций) |
| 4 | `SHT_RELA` | релокации с явным addend (`.rela.text`) |
| 8 | `SHT_NOBITS` | секция без содержимого в файле (`.bss`) |
| 9 | `SHT_REL` | релокации без addend |
| 11 | `SHT_DYNSYM` | таблица динамических символов |

`SHT_NOBITS` важен: для `.bss` `sh_size` ненулевой, но в файле эта
секция занимает 0 байт — еe содержимое создаeтся в памяти как нули.

### 19.3. Поле `sh_flags`

| Флаг | Значение | Смысл |
|------|----------|-------|
| `SHF_WRITE` | `0x1` | секция писабельна в памяти |
| `SHF_ALLOC` | `0x2` | секция занимает память при загрузке |
| `SHF_EXECINSTR` | `0x4` | секция содержит инструкции |
| `SHF_MERGE` | `0x10` | элементы можно сливать (например, строковые литералы) |
| `SHF_STRINGS` | `0x20` | содержит null-terminated строки |
| `SHF_TLS` | `0x400` | thread-local storage |

Например, `.text` имеет `SHF_ALLOC | SHF_EXECINSTR`, `.data` —
`SHF_ALLOC | SHF_WRITE`, `.bss` — то же самое, но `sh_type =
SHT_NOBITS`.

### 19.4. Стандартные имена секций

| Имя | Тип | Флаги | Содержимое |
|-----|-----|-------|------------|
| `.text` | `PROGBITS` | `A+X` | машинный код функций |
| `.rodata` | `PROGBITS` | `A` | константы, строковые литералы |
| `.data` | `PROGBITS` | `A+W` | инициализированные глобальные переменные |
| `.bss` | `NOBITS` | `A+W` | неинициализированные глобальные (зануляются) |
| `.symtab` | `SYMTAB` | — | символы для линкера/отладчика |
| `.strtab` | `STRTAB` | — | имена символов |
| `.shstrtab` | `STRTAB` | — | имена секций |
| `.rela.text` | `RELA` | — | релокации для `.text` |
| `.dynsym` / `.dynstr` | `DYNSYM` / `STRTAB` | `A` | то же, но для динамической линковки |
| `.plt` / `.got` | `PROGBITS` | `A+X` / `A+W` | таблицы для динамики |
| `.interp` | `PROGBITS` | `A` | путь к динамическому линкеру |
| `.comment`, `.debug_*` | `PROGBITS` | — | метаданные, отладка |

### 19.5. Имена секций и `.shstrtab`

В Shdr поле `sh_name` — это **не строка**, а offset внутри отдельной
секции `.shstrtab`. Сама `.shstrtab` — это просто склеенные через `\0`
имена секций:

```
.shstrtab:  \0 . t e x t \0 . d a t a \0 . b s s \0 . s h s t r t a b \0
            0  1                 7              13                   24
```

Если у секции `sh_name = 7`, еe имя начинается с offset 7 в этом
блобе — `".data"`.

Индекс самой `.shstrtab` среди секций записан в `e_shstrndx` Ehdr.

### 19.6. Связь Phdr и Shdr

Сегмент (Phdr) обычно покрывает несколько секций (Shdr). Например,
один `PT_LOAD` с `PF_R | PF_X` содержит `.text` и `.rodata`, второй
`PT_LOAD` с `PF_R | PF_W` — `.data` и `.bss`. Это правило линкер
соблюдает по умолчанию: секции с одинаковыми правами группируются в
один сегмент.

`readelf -l file` показывает обе таблицы и их соответствие.

---

## Глава 20. Виртуальные адреса, выравнивание и загрузка в память

### 20.1. Что делает execve

При запуске бинарника ядро:

1. Открывает файл, читает Ehdr.
2. Проверяет магию, класс, машину.
3. Читает Program Headers.
4. Для каждой записи `PT_LOAD` выполняет `mmap`-подобное отображение:
   указанный кусок файла (`p_offset`, `p_filesz`) проецируется по
   виртуальному адресу `p_vaddr` с правами `p_flags`.
5. Если `p_memsz > p_filesz`, разница занулена.
6. Если есть `PT_INTERP` — запускает динамический линкер, передавая
   ему управление; тот доделывает загрузку библиотек и релокации.
7. Устанавливает RIP на `e_entry` и стартует.

Вот почему важна разница между «офсетом в файле» и «виртуальным
адресом» — это один и тот же байт, но в двух разных пространствах.

### 20.2. Выравнивание на страницы

Виртуальная память на x86-64 делится на страницы по 4096 байт
(`0x1000`). Отображение файла в память (`mmap`) работает только на
границах страниц. Поэтому:

- `p_align` для `PT_LOAD` практически всегда `0x1000`;
- `p_vaddr` и `p_offset` должны иметь одинаковый остаток от деления на
  `p_align` (`p_vaddr ≡ p_offset mod 0x1000`).

Если у тебя сегмент кода идeт сразу с offset 0 и vaddr 0x400000 — оба
делятся на 0x1000, всe хорошо. Если потом сегмент данных должен лежать
по vaddr 0x402000 (на 2 страницы дальше), то его offset в файле тоже
должен быть кратен 0x1000 — обычно для этого вставляют padding нулями
между концом первого сегмента и началом второго.

### 20.3. Типичные базовые адреса

| Случай | База |
|--------|------|
| No-PIE x86-64 | `0x400000` |
| PIE / shared object | `0x0` (загрузчик прибавит свою базу) |
| ARM Linux | `0x10000` |

Базу можно выбрать любую, но традиции есть. Адреса до `0x400000`
обычно зарезервированы под нулевую страницу и ld.so.

### 20.4. Точка входа в программе

`e_entry` указывает не на `main`, а на startup-стаб (часто `_start`),
который:

1. Достаeт `argc`, `argv`, `envp` со стека.
2. Подготавливает их для C-runtime.
3. Вызывает `__libc_start_main`, который, в свою очередь, вызывает
   `main`.
4. После возврата делает `exit(returned_value)`.

В минимальном «голом» ELF без libc можно сделать `_start` вручную:
прочитать что нужно со стека и завершиться через `syscall exit`.

---

## Глава 21. Минимальный ELF-исполняемый файл

Самый маленький работоспособный ELF под Linux x86-64 содержит:

- один Ehdr (64 байта),
- один Phdr с `PT_LOAD` (56 байт),
- небольшой кусок кода.

Никакие секции не нужны: ядро их не читает.

### 21.1. Логическая раскладка

```
offset 0x0000:  Ehdr            (64 байта)
offset 0x0040:  Phdr            (56 байт)
offset 0x0078:  машинный код    (несколько байт)
конец файла
```

Виртуальная база — `0x400000`, точка входа — сразу за заголовками,
т.е. `0x400078`.

### 21.2. Код «hello, exit(42)»

Самая короткая программа — просто завершиться с кодом 42:

```
48 C7 C0 3C 00 00 00     ; mov rax, 60     (syscall: exit)
48 C7 C7 2A 00 00 00     ; mov rdi, 42
0F 05                    ; syscall
```

14 байт кода.

### 21.3. Заполнение Ehdr

```c
Elf64_Ehdr eh = {0};

eh.e_ident[EI_MAG0]    = 0x7F;
eh.e_ident[EI_MAG1]    = 'E';
eh.e_ident[EI_MAG2]    = 'L';
eh.e_ident[EI_MAG3]    = 'F';
eh.e_ident[EI_CLASS]   = ELFCLASS64;
eh.e_ident[EI_DATA]    = ELFDATA2LSB;
eh.e_ident[EI_VERSION] = EV_CURRENT;
eh.e_ident[EI_OSABI]   = ELFOSABI_SYSV;

eh.e_type      = ET_EXEC;
eh.e_machine   = EM_X86_64;
eh.e_version   = EV_CURRENT;
eh.e_entry     = 0x400000 + sizeof(Elf64_Ehdr) + sizeof(Elf64_Phdr);  // = 0x400078
eh.e_phoff     = sizeof(Elf64_Ehdr);                                  // = 64
eh.e_shoff     = 0;                                                   // секций нет
eh.e_flags     = 0;
eh.e_ehsize    = sizeof(Elf64_Ehdr);
eh.e_phentsize = sizeof(Elf64_Phdr);
eh.e_phnum     = 1;
eh.e_shentsize = 0;
eh.e_shnum     = 0;
eh.e_shstrndx  = 0;
```

### 21.4. Заполнение Phdr

```c
Elf64_Phdr ph = {0};

ph.p_type   = PT_LOAD;
ph.p_flags  = PF_R | PF_X;
ph.p_offset = 0;                          // от начала файла
ph.p_vaddr  = 0x400000;                   // база загрузки
ph.p_paddr  = 0x400000;
ph.p_filesz = total_file_size;            // Ehdr + Phdr + код
ph.p_memsz  = total_file_size;
ph.p_align  = 0x1000;
```

`p_offset = 0` и `p_vaddr = 0x400000` — оба делятся на `0x1000`,
условие `p_vaddr ≡ p_offset mod p_align` выполнено.

Обрати внимание, что **сам Ehdr и Phdr тоже попадают в загружаемый
сегмент**: `p_offset = 0` означает «отобразить файл с самого начала».
Это нормально — ядро загрузит и заголовки, и код одной операцией.

### 21.5. Запуск и проверка

После записи файла:

```sh
chmod +x mini
./mini
echo $?    # 42
```

И посмотреть структуру:

```sh
readelf -h mini   # ELF Header
readelf -l mini   # Program Headers
objdump -d -b binary -m i386:x86-64 -M intel mini
```

### 21.6. Что добавит динамика

Если же нужно использовать функции из libc, минимальный набор сегментов
расширяется:

- `PT_INTERP` со строкой `/lib64/ld-linux-x86-64.so.2`;
- `PT_DYNAMIC` с таблицей DT_NEEDED, DT_SYMTAB, DT_STRTAB, DT_RELA,
  DT_PLTGOT и т.д.;
- секции `.dynsym`, `.dynstr`, `.rela.plt`, `.plt`, `.got.plt`.

Это уже отдельная большая тема (динамическая линковка, GOT/PLT,
lazy binding) — см. ссылки в главе 23.

---

# Часть V. Справочники

## Глава 22. Полная шпаргалка

| Задача                  | Байты |
|-------------------------|-------|
| `nop`                   | `90` |
| `push rax`              | `50` |
| `pop rax`               | `58` |
| `push rbp`              | `55` |
| `pop rbp`               | `5D` |
| `push r12`              | `41 54` |
| `pop r12`               | `41 5C` |
| `mov rbp, rsp`          | `48 89 E5` |
| `mov rsp, rbp`          | `48 89 EC` |
| `mov rax, 0` (imm32)    | `48 C7 C0 00 00 00 00` |
| `xor eax, eax`          | `31 C0` |
| `mov rax, 0xFFFFFFFFFFFFFFFF` (imm64) | `48 B8 FF FF FF FF FF FF FF FF` |
| `add rsp, 8`            | `48 83 C4 08` |
| `sub rsp, 8`            | `48 83 EC 08` |
| `and rsp, -16`          | `48 83 E4 F0` |
| `inc rax`               | `48 FF C0` |
| `dec rax`               | `48 FF C8` |
| `neg rax`               | `48 F7 D8` |
| `not rax`               | `48 F7 D0` |
| `add rax, rbx`          | `48 01 D8` |
| `sub rax, rbx`          | `48 29 D8` |
| `imul rax, rbx`         | `48 0F AF C3` |
| `idiv rbx` (после `cqo`)| `48 99` + `48 F7 FB` |
| `cmp rax, rbx`          | `48 39 D8` |
| `test rax, rax`         | `48 85 C0` |
| `mov [rcx], rax`        | `48 89 01` |
| `mov rax, [rcx]`        | `48 8B 01` |
| `push qword [rcx]`      | `FF 31` |
| `lea rsp, [rbp-24]`     | `48 8D 65 E8` |
| `lea rcx, [rcx+rdi*8]`  | `48 8D 0C F9` |
| `call rel32`            | `E8 XX XX XX XX` |
| `jmp rel32`             | `E9 XX XX XX XX` |
| `je rel32`              | `0F 84 XX XX XX XX` |
| `jne rel32`             | `0F 85 XX XX XX XX` |
| `jl rel32`              | `0F 8C XX XX XX XX` |
| `jle rel32`             | `0F 8E XX XX XX XX` |
| `jg rel32`              | `0F 8F XX XX XX XX` |
| `jge rel32`             | `0F 8D XX XX XX XX` |
| `jmp [rip+rel32]`       | `FF 25 XX XX XX XX` |
| `call [rip+rel32]`      | `FF 15 XX XX XX XX` |
| `ret`                   | `C3` |
| `leave`                 | `C9` |
| `syscall`               | `0F 05` |

---

## Глава 23. Где читать дальше

### 23.1. Первоисточники

- **Intel® 64 and IA-32 Architectures Software Developer's Manual**
  (Intel SDM). Volume 2 (Instruction Set Reference) — это исчерпывающее
  описание каждой инструкции, включая опкоды, ModRM/SIB-схемы и
  семантику.
  <https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html>

- **AMD64 Architecture Programmer's Manual** — альтернативный
  первоисточник от AMD; во многом совпадает с Intel SDM, но иногда
  понятнее по тексту.
  <https://www.amd.com/system/files/TechDocs/40332.pdf> (Volume 3 — General-Purpose and System Instructions)

### 23.2. Удобные онлайн-справочники

- **Felix Cloutier's x86 Instruction Reference** — Intel SDM в виде
  HTML, с поиском по мнемоникам.
  <https://www.felixcloutier.com/x86/>

- **Online x86 Encoder/Decoder** Daniel Lemire / shell-storm и
  аналогичные — проверять руками собранные байты:
  <https://defuse.ca/online-x86-assembler.htm>
  <https://shell-storm.org/online/Online-Assembler-and-Disassembler/>

- **Compiler Explorer (godbolt.org)** — смотрите
  ассемблер с подсветкой и опционально дамп опкодов
  (`-Wa,-adhln` для GCC).
  <https://godbolt.org/>

### 23.3. ABI и системные вызовы

- **System V Application Binary Interface — AMD64 Architecture
  Processor Supplement** (psABI). Описывает соглашения вызовов на
  Linux/macOS/*BSD.
  <https://gitlab.com/x86-psABIs/x86-64-ABI>

- **Microsoft x64 calling convention** — Windows-ABI отличается
  (используются `rcx, rdx, r8, r9`, есть shadow space, нет red zone).
  <https://learn.microsoft.com/en-us/cpp/build/x64-calling-convention>

- **Linux syscall table for x86-64** — нумерация syscall'ов и
  соглашение по аргументам.
  <https://filippo.io/linux-syscall-table/>
  или `man 2 syscall`.

### 23.4. Учебные материалы

- **OSDev Wiki — X86-64 Instruction Encoding** — компактный обзор
  кодирования.
  <https://wiki.osdev.org/X86-64_Instruction_Encoding>

- **«Linux Programmer's Manual»** для конкретных syscall'ов: `man 2 read`,
  `man 2 write`, `man 2 mmap`, `man 2 execve`.

- **Eli Bendersky — Stack frame layout on x86-64**, **Position
  Independent Code (PIC) in shared libraries on x86-64** — серия статей
  про ABI, фреймы, GOT/PLT.
  <https://eli.thegreenplace.net/tag/x86>

- **Agner Fog — Optimization Manuals** (особенно «Calling conventions»
  и «Instruction tables») — практические детали по производительности
  и кодированию.
  <https://www.agner.org/optimize/>

### 23.5. Формат ELF

- **System V ABI — Generic ABI (Chapter 4: Object Files)** — базовая
  спецификация ELF, описание Ehdr/Phdr/Shdr, типов и флагов.
  <https://www.sco.com/developers/gabi/latest/contents.html>

- **System V AMD64 psABI** — дополнение для x86-64: типы релокаций
  (`R_X86_64_*`), специфика `e_machine`, размеры.
  <https://gitlab.com/x86-psABIs/x86-64-ABI>

- `man 5 elf` — краткое описание всех структур из `<elf.h>` с полями.

- **Wikipedia — Executable and Linkable Format** — хороший обзорный
  материал с диаграммами.
  <https://en.wikipedia.org/wiki/Executable_and_Linkable_Format>

- **Brian Raiter — A Whirlwind Tutorial on Creating Really Teensy ELF
  Executables for Linux** — классическая статья про минимальные ELF.
  <https://www.muppetlabs.com/~breadbox/software/tiny/teensy.html>

- **«Linkers and Loaders»** John R. Levine — книга, в которой подробно
  разбираются объектные форматы, включая ELF; до сих пор актуальна.

- `readelf -a file`, `objdump -h file`, `eu-readelf` — инструменты для
  разбора готовых файлов.

### 23.6. Инструменты для проверки своего кодгена

- `nasm`/`yasm` + `ndisasm` — собрать `.asm`, дизассемблировать и
  сравнить с тем, что генерирует ваш кодоген:

  ```sh
  nasm -f bin -o test.bin test.asm
  ndisasm -b 64 test.bin
  ```

- `objdump -d -M intel` — дизассемблер из binutils. Удобно сравнивать
  свои байты с эталоном:

  ```sh
  echo -ne '\x48\x01\xD8' > t.bin
  objdump -D -b binary -m i386:x86-64 -M intel t.bin
  ```

- `gdb` с командой `disas /r` — выводит и опкоды, и мнемонику:

  ```
  (gdb) disas /r main
  ```

- `xxd`, `hexdump -C` — посмотреть свой бинарник побайтно.
